# Setup

Build and package project:

```Shell
mvn package
```

Run DepView:

```Shell
bin/depview <CONF>
```

View result:

```Shell
bin/serve output/<NAME>
```

Open [localhost:8000](localhost:8000)
